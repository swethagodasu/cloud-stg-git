name: Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Sync
        uses: swethagodasu/cloud-stg-git@1.3
        with:
          secrets: ${{ secrets.google_service_account_credentials }}
          bucket: 'git-to-storage-sync'
          exclude: '.*\.md$|\.gitignore$|\.git/.*$|\.github/.*$'

